<div class="mt-4">
    <div class="row">
        <div class="col-lg-3">
            <button>
                Học ngay
            </button>
        </div>
        <div class="col-lg-9">
            <h2>
                {{course.name}}
            </h2>
            <iframe width="560" height="315" src="http://www.youtube.com/embed/{{course.videoId}}"></iframe>
            <p>{{course.des}}</p>
        </div>
    </div>
</div>